<%- include('partials/head') %>
<% if (user) { %><%- include('partials/shell-open', { user, active: '' }) %><% } %>
  <div class="empty-state">
    <h1 style="color:var(--text); font-size:20px;"><%= title %></h1>
    <p><%= message %></p>
    <a class="btn" style="width:auto; display:inline-block; padding:10px 20px;" href="/dashboard">Back to dashboard</a>
  </div>
<% if (user) { %><%- include('partials/shell-close') %><% } else { %></body></html><% } %>
